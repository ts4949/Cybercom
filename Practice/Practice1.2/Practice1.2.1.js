//Create an object to store information about a person (e.g., name, age, address, etc.).
// Write code to access and modify the object's properties.

const person = {
  name: 'Tathya',
  age: 22,
  city: 'Rajkot',
};

console.log(person.name); 
console.log(person.age); 
console.log(person.city); 

person.name = 'Tathya Shukla';
person.age = 23;
person.city = 'Ahmedabad';

console.log(person.name);
console.log(person.age); 
console.log(person.city); 
