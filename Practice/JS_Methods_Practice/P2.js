//Create an object with one Boolean property. Set it to 1 and 0 vice versa and use it in if block as condition and observe the output !

A = { name: "abc" , age: 0 };
if(A.age)
{ console.log("if block")}
else
{ console.log("else block")}